spring:
  main:
    web-application-type: none
    banner-mode: off
    lazy-initialization: true

logging.level:
  root: warn
  ru.itmo: info
  edu.stanford.nlp.process.PTBLexer: error

stand:
  app:
    base-path: "."
    neighbours-algorithm:
      token-batch-size: 5
      bert-model-type: TINY
      documents-count: 500000
      embedding-storage: HNSW
    bert-multi-token:
      token-batch-size: 5
